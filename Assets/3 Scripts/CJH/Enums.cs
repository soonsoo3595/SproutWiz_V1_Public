using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Enum 형 변수는 여기에 정의함

public class Enums : MonoBehaviour
{
   
}

public enum ItemType { Seed, Scroll, Harvest, Material}

public enum Element { Non, Fire, Water, Grass }

public enum Growth { Non, Seed, Sprout, Bloom}

public enum ButtonType { Popup, Back }

public enum ItemTier { One, Two, Three}
